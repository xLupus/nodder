const { Comment } = require('../../models/index');

class CreateCommentController{

    /**
     * todo - Alterar para pegar o ID do usuario logado
     * @param {*} req 
     * @param {*} res 
     */
    store(req, res){

    }
}

module.exports = CreateCommentController;